import { randomUUID } from "crypto";
import { Student, Cluster, ClusterManager } from "./student.js";

const cluster1 = new Cluster(randomUUID(), {
  age: 23,
});
const cluster2 = new Cluster(randomUUID(), {
  age: 29,
});

const clusterManager = new ClusterManager([cluster1, cluster2]);

const unicesumarStudent7: Student = {
  age: 57,
};

clusterManager.add(unicesumarStudent7);
const unicesumarStudent8: Student = {
  age: 66,
};

clusterManager.add(unicesumarStudent8);
const unicesumarStudent9: Student = {
  age: 75,
};

clusterManager.add(unicesumarStudent9);
const unicesumarStudent10: Student = {
  age: 23,
};

clusterManager.add(unicesumarStudent10);
const unicesumarStudent11: Student = {
  age: 44,
};

clusterManager.add(unicesumarStudent11);
const unicesumarStudent12: Student = {
  age: 50,
};

clusterManager.add(unicesumarStudent12);
const unicesumarStudent13: Student = {
  age: 100,
};

clusterManager.add(unicesumarStudent13);
const unicesumarStudent14: Student = {
  age: 24,
};

clusterManager.add(unicesumarStudent14);
const unicesumarStudent15: Student = {
  age: 19,
};

clusterManager.add(unicesumarStudent15);
const unicesumarStudent16: Student = {
  age: 92,
};

clusterManager.add(unicesumarStudent16);
const unicesumarStudent17: Student = {
  age: 28,
};

clusterManager.add(unicesumarStudent17);
const unicesumarStudent18: Student = {
  age: 70,
};

clusterManager.add(unicesumarStudent18);
const unicesumarStudent19: Student = {
  age: 42,
};

clusterManager.add(unicesumarStudent19);
const unicesumarStudent20: Student = {
  age: 20,
};

clusterManager.add(unicesumarStudent20);
const unicesumarStudent21: Student = {
  age: 26,
};

clusterManager.add(unicesumarStudent21);
const unicesumarStudent22: Student = {
  age: 45,
};

clusterManager.add(unicesumarStudent22);
const unicesumarStudent23: Student = {
  age: 30,
};

clusterManager.add(unicesumarStudent23);
const unicesumarStudent24: Student = {
  age: 83,
};

clusterManager.add(unicesumarStudent24);
const unicesumarStudent25: Student = {
  age: 83,
};

clusterManager.add(unicesumarStudent25);
const unicesumarStudent26: Student = {
  age: 36,
};

clusterManager.add(unicesumarStudent26);
const unicesumarStudent27: Student = {
  age: 54,
};

clusterManager.add(unicesumarStudent27);
const unicesumarStudent28: Student = {
  age: 67,
};

clusterManager.add(unicesumarStudent28);
const unicesumarStudent29: Student = {
  age: 66,
};

clusterManager.add(unicesumarStudent29);
const unicesumarStudent30: Student = {
  age: 44,
};

clusterManager.add(unicesumarStudent30);
const unicesumarStudent31: Student = {
  age: 41,
};

clusterManager.add(unicesumarStudent31);
const unicesumarStudent32: Student = {
  age: 49,
};

clusterManager.add(unicesumarStudent32);
const unicesumarStudent33: Student = {
  age: 42,
};

clusterManager.add(unicesumarStudent33);
const unicesumarStudent34: Student = {
  age: 70,
};

clusterManager.add(unicesumarStudent34);
const unicesumarStudent35: Student = {
  age: 31,
};

clusterManager.add(unicesumarStudent35);
const unicesumarStudent36: Student = {
  age: 52,
};

clusterManager.add(unicesumarStudent36);
const unicesumarStudent37: Student = {
  age: 66,
};

clusterManager.add(unicesumarStudent37);
const unicesumarStudent38: Student = {
  age: 83,
};

clusterManager.add(unicesumarStudent38);
const unicesumarStudent39: Student = {
  age: 77,
};

clusterManager.add(unicesumarStudent39);
const unicesumarStudent40: Student = {
  age: 58,
};

clusterManager.add(unicesumarStudent40);
const unicesumarStudent41: Student = {
  age: 30,
};

clusterManager.add(unicesumarStudent41);
const unicesumarStudent42: Student = {
  age: 68,
};

clusterManager.add(unicesumarStudent42);
const unicesumarStudent43: Student = {
  age: 90,
};

clusterManager.add(unicesumarStudent43);
const unicesumarStudent44: Student = {
  age: 81,
};

clusterManager.add(unicesumarStudent44);
const unicesumarStudent45: Student = {
  age: 70,
};

clusterManager.add(unicesumarStudent45);
const unicesumarStudent46: Student = {
  age: 34,
};

clusterManager.add(unicesumarStudent46);
const unicesumarStudent47: Student = {
  age: 35,
};

clusterManager.add(unicesumarStudent47);
const unicesumarStudent48: Student = {
  age: 59,
};

clusterManager.add(unicesumarStudent48);
const unicesumarStudent49: Student = {
  age: 100,
};

clusterManager.add(unicesumarStudent49);
const unicesumarStudent50: Student = {
  age: 73,
};

clusterManager.add(unicesumarStudent50);
const unicesumarStudent51: Student = {
  age: 76,
};

clusterManager.add(unicesumarStudent51);
const unicesumarStudent52: Student = {
  age: 83,
};

clusterManager.add(unicesumarStudent52);
const unicesumarStudent53: Student = {
  age: 53,
};

clusterManager.add(unicesumarStudent53);
const unicesumarStudent54: Student = {
  age: 29,
};

clusterManager.add(unicesumarStudent54);
const unicesumarStudent55: Student = {
  age: 64,
};

clusterManager.add(unicesumarStudent55);
const unicesumarStudent56: Student = {
  age: 22,
};

clusterManager.add(unicesumarStudent56);
const unicesumarStudent57: Student = {
  age: 72,
};

clusterManager.add(unicesumarStudent57);
const unicesumarStudent58: Student = {
  age: 88,
};

clusterManager.add(unicesumarStudent58);
const unicesumarStudent59: Student = {
  age: 54,
};

clusterManager.add(unicesumarStudent59);
const unicesumarStudent60: Student = {
  age: 97,
};

clusterManager.add(unicesumarStudent60);
const unicesumarStudent61: Student = {
  age: 37,
};

clusterManager.add(unicesumarStudent61);
const unicesumarStudent62: Student = {
  age: 38,
};

clusterManager.add(unicesumarStudent62);
const unicesumarStudent63: Student = {
  age: 87,
};

clusterManager.add(unicesumarStudent63);
const unicesumarStudent64: Student = {
  age: 61,
};

clusterManager.add(unicesumarStudent64);
const unicesumarStudent65: Student = {
  age: 53,
};

clusterManager.add(unicesumarStudent65);
const unicesumarStudent66: Student = {
  age: 43,
};

clusterManager.add(unicesumarStudent66);
const unicesumarStudent67: Student = {
  age: 73,
};

clusterManager.add(unicesumarStudent67);
const unicesumarStudent68: Student = {
  age: 42,
};

clusterManager.add(unicesumarStudent68);
const unicesumarStudent69: Student = {
  age: 50,
};

clusterManager.add(unicesumarStudent69);
const unicesumarStudent70: Student = {
  age: 51,
};

clusterManager.add(unicesumarStudent70);
const unicesumarStudent71: Student = {
  age: 64,
};

clusterManager.add(unicesumarStudent71);
const unicesumarStudent72: Student = {
  age: 33,
};

clusterManager.add(unicesumarStudent72);
const unicesumarStudent73: Student = {
  age: 78,
};

clusterManager.add(unicesumarStudent73);
const unicesumarStudent74: Student = {
  age: 20,
};

clusterManager.add(unicesumarStudent74);
const unicesumarStudent75: Student = {
  age: 56,
};

clusterManager.add(unicesumarStudent75);
const unicesumarStudent76: Student = {
  age: 75,
};

clusterManager.add(unicesumarStudent76);
const unicesumarStudent77: Student = {
  age: 71,
};

clusterManager.add(unicesumarStudent77);
const unicesumarStudent78: Student = {
  age: 99,
};

clusterManager.add(unicesumarStudent78);
const unicesumarStudent79: Student = {
  age: 61,
};

clusterManager.add(unicesumarStudent79);
const unicesumarStudent80: Student = {
  age: 64,
};

clusterManager.add(unicesumarStudent80);
const unicesumarStudent81: Student = {
  age: 91,
};

clusterManager.add(unicesumarStudent81);
const unicesumarStudent82: Student = {
  age: 37,
};

clusterManager.add(unicesumarStudent82);
const unicesumarStudent83: Student = {
  age: 31,
};

clusterManager.add(unicesumarStudent83);
const unicesumarStudent84: Student = {
  age: 47,
};

clusterManager.add(unicesumarStudent84);
const unicesumarStudent85: Student = {
  age: 97,
};

clusterManager.add(unicesumarStudent85);
const unicesumarStudent86: Student = {
  age: 19,
};

clusterManager.add(unicesumarStudent86);
const unicesumarStudent87: Student = {
  age: 64,
};

clusterManager.add(unicesumarStudent87);
const unicesumarStudent88: Student = {
  age: 46,
};

clusterManager.add(unicesumarStudent88);
const unicesumarStudent89: Student = {
  age: 28,
};

clusterManager.add(unicesumarStudent89);
const unicesumarStudent90: Student = {
  age: 87,
};

clusterManager.add(unicesumarStudent90);
const unicesumarStudent91: Student = {
  age: 54,
};

clusterManager.add(unicesumarStudent91);
const unicesumarStudent92: Student = {
  age: 35,
};

clusterManager.add(unicesumarStudent92);
const unicesumarStudent93: Student = {
  age: 44,
};

clusterManager.add(unicesumarStudent93);
const unicesumarStudent94: Student = {
  age: 35,
};

clusterManager.add(unicesumarStudent94);
const unicesumarStudent95: Student = {
  age: 93,
};

clusterManager.add(unicesumarStudent95);
const unicesumarStudent96: Student = {
  age: 24,
};

clusterManager.add(unicesumarStudent96);
const unicesumarStudent97: Student = {
  age: 59,
};

clusterManager.add(unicesumarStudent97);
const unicesumarStudent98: Student = {
  age: 54,
};

clusterManager.add(unicesumarStudent98);
const unicesumarStudent99: Student = {
  age: 94,
};

clusterManager.add(unicesumarStudent99);
const unicesumarStudent100: Student = {
  age: 59,
};

clusterManager.add(unicesumarStudent100);
const unicesumarStudent101: Student = {
  age: 42,
};

clusterManager.add(unicesumarStudent101);
const unicesumarStudent102: Student = {
  age: 47,
};

clusterManager.add(unicesumarStudent102);
const unicesumarStudent103: Student = {
  age: 21,
};

clusterManager.add(unicesumarStudent103);
const unicesumarStudent104: Student = {
  age: 30,
};

clusterManager.add(unicesumarStudent104);
const unicesumarStudent105: Student = {
  age: 50,
};

clusterManager.add(unicesumarStudent105);
const unicesumarStudent106: Student = {
  age: 81,
};

clusterManager.add(unicesumarStudent106);

console.dir(clusterManager.clusters, { depth: null });
